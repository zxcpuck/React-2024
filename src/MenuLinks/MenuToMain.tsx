import React from 'react';
import {Link} from "react-router-dom";

const MenuToMain = () => {
    return (
        <div>
            <Link to='/'>Home page</Link>
        </div>
    );
};

export default MenuToMain;